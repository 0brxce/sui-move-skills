# Reports

This directory stores generated Sui Move audit reports produced by the `sui-move-auditor` skill.

Default output files should be written here unless the user explicitly requests another path.
